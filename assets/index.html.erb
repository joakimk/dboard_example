<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>Dashboard</title>

  <script src="jquery.js"></script>
  <script src="dashboard.js"></script>
  <script type="text/javascript">
    $(document).ready(function() {
        window.dashboard_version = '<%= ENV['COMMIT_HASH'] || 'unversioned' %>';
        dashboard = new window.Dashboard();

        var CpuLoadWidget = function()
        {
          this.render = function(data)
          {
            $("#cpu_load")[0].style.height = data.load + "%"
          }
        }

        dashboard.addWidget('cpu', CpuLoadWidget);
        dashboard.run();
    });
  </script>
</head>

<body style="background-color: black; color: white">
  <div id="content">
    <h3>Current reported CPU usage:</h3>
    <div style="width: 100px; height: 600px;">
      <div id="cpu_load" style="background-color: #1111FF; width: 100px; border-radius: 5px">&nbsp;</div>
    </div>
  </div>
</body>
</html>
